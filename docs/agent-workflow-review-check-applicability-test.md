# Independent Review Check Applicability Test

This temporary document verifies that an ordinary, human-authored pull request
targeting `dev` receives an App-owned `AI Independent Review` check with a
successful `not applicable` conclusion.

The pull request is not part of the Agent implementation path and must not
trigger model review.
